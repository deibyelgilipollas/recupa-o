function convertirMoneda() {
    // Obtener el monto ingresado
    var montoEndolares = document.getElementById("monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montoEndolares = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.048;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montoEndolares * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montoEndolares = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.048;

    // Calcular el monto en Euros
    var montoEnpesocolombiano= montoEndolares * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = ("Monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montoEndolares = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.048;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montoEndolares * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = "Monto en dolares: " + montoEnpesocolombiano.toFixed(3) + ("monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montoEndolares = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.048;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montoEndolares * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = "Monto en peso colobiano: " + montoEnpesocolombiano.toFixed(3) + " cop";
}