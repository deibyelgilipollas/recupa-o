function convertirMoneda() {
    // Obtener el monto ingresado
    var montochileno = document.getElementById("monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montoenreal = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.61;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montochileno * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montochileno = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.61;

    // Calcular el monto en Euros
    var montoEnpesocolombiano= montochileno * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = ("Monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montochileno = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.61;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montochileno * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = "Monto en reales: " + montoEnpesocolombiano.toFixed(2) + ("monto")
}
function convertirMoneda() {
    // Obtener el monto ingresado
    var montochileno = document.getElementById("monto").value;

    // Tasa de cambio (puedes actualizar este valor según la tasa actual)
    var tasaDeCambio = 4.61;

    // Calcular el monto en Euros
    var montoEnpesocolombiano = montochileno * tasaDeCambio;

    // Mostrar el resultado
    document.getElementById("resultado").innerHTML = "Monto en peso colobiano: " + montoEnpesocolombiano.toFixed(2) + " cop";
}